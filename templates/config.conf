# -----------------------------------------------------------------------------
# Lite Server Monitor (LSM) - Core Configuration
# -----------------------------------------------------------------------------

# Имя хоста (если пустое, подтягивается автоматически из системы)
LSM_HOSTNAME=""

# Уровень детализации логов (DEBUG | INFO | WARN | ERROR)
LOG_LEVEL="INFO"

# Срок хранения логов (в днях)
LOG_RETENTION_DAYS=14
